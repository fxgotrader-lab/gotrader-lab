TWELVE_DATA_API_KEY=
FMP_API_KEY=
GOTRADER_MODE=paper

# MT5 read-only local market-data stack.
# The GoTrader wrapper on 7341 proxies only safe quote/candle/symbol routes to this upstream.
# Set MT5_READONLY_DISABLE_DEFAULT_UPSTREAM=true only when you intentionally want contract-stub mode.
MT5_READONLY_UPSTREAM_BASE_URL=http://127.0.0.1:8000
MT5_READONLY_UPSTREAM_TRANSPORT=rest
MT5_READONLY_UPSTREAM_QUOTE_PATH=/api/v1/market/price
MT5_READONLY_UPSTREAM_CANDLES_PATH=/api/v1/market/candles/latest
MT5_READONLY_UPSTREAM_CANDLES_RANGE_PATH=
MT5_READONLY_UPSTREAM_SYMBOLS_PATH=/api/v1/market/symbols
MT5_READONLY_REQUESTED_SYMBOL=MNQ
MT5_READONLY_BROKER_SYMBOL=USTECH
MT5_READONLY_DEFAULT_SYMBOL=USTECH

# Optional local LLM advisory tuning.
# Health/status checks stay fast in code; this only controls advisory requests.
LLM_ADVISORY_TIMEOUT_MS=30000
LLM_ADVISORY_MODEL=

# Optional OpenClaw advisory/calibration endpoint.
# Browser-visible local endpoint only; do not put secrets here.
OPENCLAW_ADVISORY_URL=http://127.0.0.1:8797/gotrader/advisory
OPENCLAW_ADVISORY_TIMEOUT_MS=30000
# Optional for scripts/local proxies only. Do not expose this as VITE_* or paste it into the frontend.
OPENCLAW_ADVISORY_TOKEN=

# Optional local tuning for Twelve Data scripts.
TWELVE_DATA_TIMEOUT_MS=10000
TWELVE_DATA_CACHE_TTL_MS=30000
TWELVE_DATA_MIN_REQUEST_INTERVAL_MS=850

# Optional local tuning for FMP market-context scripts.
FMP_TIMEOUT_MS=10000
FMP_MIN_REQUEST_INTERVAL_MS=1000
FMP_ECONOMIC_CACHE_TTL_MS=21600000
FMP_NEWS_CACHE_TTL_MS=600000
FMP_SYMBOL_NEWS_CACHE_TTL_MS=1200000
